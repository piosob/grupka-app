---
import type { EventPreview } from '../types/landingPage';
import { Card, CardHeader, CardTitle, CardContent, CardDescription } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';

interface Props {
    event: EventPreview;
}

const { event } = Astro.props;
---

<section class="py-16 px-8 bg-gray-800 text-white">
    <h2 class="text-4xl font-bold text-center mb-12">Przykładowe wydarzenie</h2>
    <div class="max-w-md mx-auto">
        <Card class="bg-gray-700 border-gray-600">
            <CardHeader>
                <CardTitle class="text-white">🎉 {event.title}</CardTitle>
                <CardDescription class="text-gray-300">{event.dateLabel}</CardDescription>
            </CardHeader>
            <CardContent>
                <p class="mb-4 text-gray-200">{event.summary}</p>
                <Badge variant="secondary" class="mb-4">{event.hiddenThreadLabel}</Badge>
                <div class="space-y-3">
                    {event.comments.map((comment) => (
                        <div class="bg-gray-800 p-3 rounded-md">
                            <p class="text-sm font-semibold text-gray-100">{comment.authorLabel}</p>
                            <p class="text-sm text-gray-300">{comment.text}</p>
                        </div>
                    ))}
                </div>
            </CardContent>
        </Card>
    </div>
</section>

