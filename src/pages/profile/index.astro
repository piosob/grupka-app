---
import MainLayout from '../../layouts/MainLayout.astro';
import { ProfileContainer } from '../../components/react/profile/ProfileContainer';

const user = Astro.locals.user;

if (!user) {
    return Astro.redirect('/login');
}
---

<MainLayout title="Mój profil">
    <ProfileContainer userEmail={user.email} client:load />
</MainLayout>
